import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Leave a Testimonial — Tebatso Seshayi",
  description: "Worked with Tebatso? Share a quick testimonial.",
};

export default function TestimonialSubmitLayout({ children }: { children: React.ReactNode }) {
  return children;
}